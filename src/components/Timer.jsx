import React from "react";

const Timer = () => {
    return (
        <div>
            This will count down from default starting time and increase timer
            as per the rewards
        </div>
    );
};

export default Timer;
