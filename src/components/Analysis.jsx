import React from "react";

const Analysis = () => {
    return (
        <div>
            Here typing speed will be shown and also speed plot for words will
            be shown
        </div>
    );
};

export default Analysis;
